import 'package:flutter/material.dart';

class FaouriteScreen extends StatelessWidget {
  const FaouriteScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Favourite'),
      ),
    );
  }
}
